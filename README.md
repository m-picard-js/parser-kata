# JavaScript version of Hardis file parsing study case

Make sure you have node.js, npm and mocha installed. Then in this directory:

    # To install mocha in node_modules:
    npm install

    # To run unit tests:
    mocha

    # To run parser
    node index.js <source> <format> <target>

    # To display help
    node index.js -h
